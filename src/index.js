import React, { Component } from 'react';
import ReactDOM from "react-dom";

class App extends Component {
	render() {
		return (
			<div>Add reusable components to render</div>
		);
	}
}

ReactDOM.render(<App/>,document.getElementById("root"));
